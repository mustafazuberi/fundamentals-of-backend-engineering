# Long polling exercises

1. Design an API for `GET /events` using a cursor (`since`) and a server timeout.
2. List 5 failure modes in production for long polling and how you’d mitigate each.
3. Compare long polling vs SSE vs WebSockets (choose one scenario for each).

